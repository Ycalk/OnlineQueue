diff --git a/services/core/src/core/main.py b/services/core/src/core/main.py
index 81e2ae4..2b2e647 100644
--- a/services/core/src/core/main.py
+++ b/services/core/src/core/main.py
@@ -10,7 +10,7 @@ from shared.providers import (
     LoggingProvider,
     UserProvider,
 )
-from modules.user.adapters.inbound.rest import auth_router
+from modules.user.adapters.inbound.rest import auth_router, user_router
 from shared.logging import setup_logging
 
 
@@ -42,3 +42,4 @@ setup_dishka(container=container, app=app)
 
 
 app.include_router(auth_router, prefix=settings.api_prefix)
+app.include_router(user_router, prefix=settings.api_prefix)
diff --git a/services/core/src/modules/user/adapters/inbound/rest/__init__.py b/services/core/src/modules/user/adapters/inbound/rest/__init__.py
index 5e922de..896352c 100644
--- a/services/core/src/modules/user/adapters/inbound/rest/__init__.py
+++ b/services/core/src/modules/user/adapters/inbound/rest/__init__.py
@@ -1,4 +1,5 @@
 from .auth_router import router as auth_router
+from .user_router import router as user_router
 
 
-__all__ = ["auth_router"]
+__all__ = ["auth_router", "user_router"]
diff --git a/services/core/src/modules/user/adapters/inbound/rest/auth_router.py b/services/core/src/modules/user/adapters/inbound/rest/auth_router.py
index 5223e5c..4f584de 100644
--- a/services/core/src/modules/user/adapters/inbound/rest/auth_router.py
+++ b/services/core/src/modules/user/adapters/inbound/rest/auth_router.py
@@ -11,9 +11,10 @@ from shared.adapters import (
     TokenExpiredError,
     TokenType,
     HTTPError,
+    MessageResponse,
 )
 from core.settings import settings
-from .dto import RegisterRequest, LoginRequest, TokenResponse, MessageResponse
+from .dto import RegisterRequest, LoginRequest, TokenResponse
 
 router = APIRouter(prefix="/auth", tags=["authentication"], route_class=DishkaRoute)
 
@@ -117,13 +118,16 @@ async def login(
         )
 
 
-@router.post(
+@router.get(
     "/token",
     response_model=TokenResponse,
     responses={
         status.HTTP_401_UNAUTHORIZED: {
             "model": HTTPError,
-            "description": "Refresh токена нет в куках / невалиден / истек. После этого нужно залогиниться заново",
+            "description": (
+                "Refresh токена нет в куках / невалиден / истек. "
+                "После этого нужно залогиниться заново"
+            ),
         }
     },
 )
diff --git a/services/core/src/modules/user/adapters/inbound/rest/dto.py b/services/core/src/modules/user/adapters/inbound/rest/dto.py
index 027b466..9edb563 100644
--- a/services/core/src/modules/user/adapters/inbound/rest/dto.py
+++ b/services/core/src/modules/user/adapters/inbound/rest/dto.py
@@ -2,19 +2,17 @@ from pydantic import BaseModel, Field
 from typing import Literal
 
 
-class RegisterRequest(BaseModel):
+class LoginRequest(BaseModel):
     email: str = Field(description="Электронная почта", examples=["abcdef@example.com"])
     password: str = Field(description="Пароль, который ввел пользователь")
+
+
+class RegisterRequest(LoginRequest):
     first_name: str = Field(description="Имя пользователя")
     last_name: str = Field(description="Фамилия пользователя")
     patronymic: str | None = Field(None, description="Отчество пользователя")
 
 
-class LoginRequest(BaseModel):
-    email: str = Field(description="Электронная почта", examples=["abcdef@example.com"])
-    password: str = Field(description="Пароль, который ввел пользователь")
-
-
 class TokenResponse(BaseModel):
     access_token: str = Field(
         description="Токен доступа, который нужен для аутентификации (его нужно передавать в header)"
@@ -25,5 +23,17 @@ class TokenResponse(BaseModel):
     )
 
 
-class MessageResponse(BaseModel):
-    message: str
+class UpdateEmailRequest(BaseModel):
+    new_email: str = Field(description="Новый email", examples=["abcdef@example.com"])
+    current_password: str = Field(description="Текущий пароль пользователя")
+
+
+class UpdateNameRequest(BaseModel):
+    first_name: str | None = Field(description="Новое имя пользователя")
+    last_name: str | None = Field(description="Новая фамилия пользователя")
+    patronymic: str | None = Field(description="Новое отчество пользователя")
+
+
+class UpdatePasswordRequest(BaseModel):
+    old_password: str = Field(description="Текущий пароль пользователя")
+    new_password: str = Field(description="Новый пароль пользователя")
diff --git a/services/core/src/modules/user/application/use_cases/change_email.py b/services/core/src/modules/user/application/use_cases/change_email.py
index c7f6db7..50c3dbc 100644
--- a/services/core/src/modules/user/application/use_cases/change_email.py
+++ b/services/core/src/modules/user/application/use_cases/change_email.py
@@ -17,6 +17,6 @@ class ChangeEmail(ApplicationUseCase, IChangeEmail):
         user = await self._user_repository.find_by_id(command.user_id)
         if user is None:
             raise UserNotFoundError(f"User with id {command.user_id} not found")
-        user.change_email(command.new_email)
+        user.change_email(command.new_email, command.password)
         await self._user_repository.save(user)
         await self._publish_events(user)
diff --git a/services/core/src/modules/user/domain/aggregates/model.py b/services/core/src/modules/user/domain/aggregates/model.py
index fc4d399..3c85715 100644
--- a/services/core/src/modules/user/domain/aggregates/model.py
+++ b/services/core/src/modules/user/domain/aggregates/model.py
@@ -9,7 +9,11 @@ from modules.user.domain.events import (
     EmailChanged,
     NameChanged,
 )
-from modules.user.domain.errors import InvalidPasswordError
+from modules.user.domain.errors import (
+    InvalidPasswordError,
+    SamePasswordError,
+    SameEmailError,
+)
 from typing import Self
 
 
@@ -41,10 +45,19 @@ class User(AggregateRoot):
         if not self.hashed_password.verify(old_password):
             raise InvalidPasswordError("Invalid password")
 
+        if old_password == new_password:
+            raise SamePasswordError("New password must be different from old password")
+
         self.hashed_password = HashedPassword.from_plain_password(new_password)
         self._add_event(PasswordChanged(user_id=self.id, email=self.email))
 
-    def change_email(self, new_email: Email) -> None:
+    def change_email(self, new_email: Email, password: str) -> None:
+        if not self.hashed_password.verify(password):
+            raise InvalidPasswordError("Invalid password")
+
+        if new_email == self.email:
+            raise SameEmailError("New email must be different from old email")
+
         event = EmailChanged(user_id=self.id, new_email=new_email, old_email=self.email)
         self.email = new_email
         self._add_event(event)
diff --git a/services/core/src/modules/user/domain/commands/change_email.py b/services/core/src/modules/user/domain/commands/change_email.py
index 819b366..b6e677f 100644
--- a/services/core/src/modules/user/domain/commands/change_email.py
+++ b/services/core/src/modules/user/domain/commands/change_email.py
@@ -6,3 +6,4 @@ from modules.user.domain.value_objects import Email
 class ChangeEmail(BaseModel):
     user_id: UserId
     new_email: Email
+    password: str
diff --git a/services/core/src/modules/user/domain/errors.py b/services/core/src/modules/user/domain/errors.py
index b5bb9aa..4cc8334 100644
--- a/services/core/src/modules/user/domain/errors.py
+++ b/services/core/src/modules/user/domain/errors.py
@@ -5,3 +5,9 @@ class WeakPasswordError(DomainError): ...
 
 
 class InvalidPasswordError(DomainError): ...
+
+
+class SamePasswordError(DomainError): ...
+
+
+class SameEmailError(DomainError): ...
diff --git a/services/core/src/shared/adapters/__init__.py b/services/core/src/shared/adapters/__init__.py
index 995c318..3fbf50e 100644
--- a/services/core/src/shared/adapters/__init__.py
+++ b/services/core/src/shared/adapters/__init__.py
@@ -1,5 +1,13 @@
 from .in_memory_publisher import InMemoryEventPublisher
-from .rest import TokenType, InvalidTokenError, TokenExpiredError, JWTService, HTTPError
+from .rest import (
+    TokenType,
+    InvalidTokenError,
+    TokenExpiredError,
+    JWTService,
+    HTTPError,
+    MessageResponse,
+    get_current_user_id,
+)
 
 
 __all__ = [
@@ -9,4 +17,6 @@ __all__ = [
     "JWTService",
     "TokenType",
     "HTTPError",
+    "MessageResponse",
+    "get_current_user_id",
 ]
diff --git a/services/core/src/shared/adapters/rest/__init__.py b/services/core/src/shared/adapters/rest/__init__.py
index e5b1e1f..bff70e0 100644
--- a/services/core/src/shared/adapters/rest/__init__.py
+++ b/services/core/src/shared/adapters/rest/__init__.py
@@ -1,5 +1,6 @@
 from .jwt_service import TokenType, InvalidTokenError, TokenExpiredError, JWTService
-from .http_error import HTTPError
+from .default_models import HTTPError, MessageResponse
+from .auth import get_current_user_id
 
 
 __all__ = [
@@ -8,4 +9,6 @@ __all__ = [
     "JWTService",
     "TokenType",
     "HTTPError",
+    "MessageResponse",
+    "get_current_user_id",
 ]
diff --git a/services/core/src/shared/adapters/rest/http_error.py b/services/core/src/shared/adapters/rest/http_error.py
deleted file mode 100644
index 6487ccc..0000000
--- a/services/core/src/shared/adapters/rest/http_error.py
+++ /dev/null
@@ -1,5 +0,0 @@
-from pydantic import BaseModel, Field
-
-
-class HTTPError(BaseModel):
-    detail: str = Field(description="Детали ошибки")
